# 慢慢瘦 Prompt 文档

## 项目名称

慢慢瘦 - 减脂健康助手

## 核心定位

构建一个减脂健康管理单页应用（SPA）。应用所有核心功能都在一个页面内完成，通过底部 Tab 导航切换不同功能区，不发生整页刷新，整体体验接近移动端原生 App。

## 目标用户

面向希望记录饮食、运动、饮水和体重变化的减脂用户。应用应帮助用户理解每日热量摄入、运动消耗、净摄入、目标热量和体重变化趋势。

## 技术要求

- 前端：`index.html` + 原生 JavaScript + Tailwind CSS CDN。
- 后端：Node.js + Express。
- 数据库：SQLite 文件数据库，无需额外数据库服务。
- 图表：Chart.js CDN，用于热量环图和体重趋势折线图。
- 认证：使用 `express-session` 维护登录状态，不使用 JWT。
- 密码：注册时校验密码长度不少于 6 位，使用 bcrypt 哈希存储。
- 启动：执行 `npm install` 后运行 `node server.js`，浏览器访问 `http://localhost:3000`。

## 应用结构

应用只有一个入口页面 `public/index.html`。

页面分为两种状态：

- 未登录：展示登录和注册表单。
- 已登录：展示主应用，包括仪表盘、饮食、运动、体重、饮水、历史等功能 Tab。

已登录后的所有功能通过 Tab 切换显示或隐藏。每个 Tab 对应一个页面区域，使用 hash 路由或前端状态控制当前显示区域。

## 功能需求

### 认证模块

- 用户注册：输入用户名和密码，密码不少于 6 位。
- 用户登录：输入用户名和密码。
- 用户退出：清除当前 Session。
- 未登录用户不能访问需要登录的 API。

### 仪表盘 Tab

- 展示今日热量环图：今日已摄入热量 vs 目标热量。
- 展示今日摘要：剩余热量、已摄入、运动消耗、净摄入。
- 展示饮水进度：当前饮水量 / 2000ml 目标。
- 展示每日打卡状态：饮食、运动、饮水。
- 展示体重目标进度：当前体重到目标体重的变化进度。
- 根据热量缺口估算减脂周期。

### 饮食 Tab

- 内置常见食物热量库，支持按名称搜索。
- 食物搜索结果展示食物名称和每 100g 热量。
- 添加饮食记录时选择食物、输入食用量、选择餐次，并自动计算总热量。
- 支持自定义食物：输入名称、每 100g 热量、食用量和餐次。
- 支持将自定义食物保存到“我的食物”，后续可重复使用。
- 今日饮食记录按餐次分组展示。
- 支持删除单条饮食记录。
- 根据目标热量提供早 30%、午 40%、晚 30% 的饮食建议。

### 运动 Tab

- 内置运动 MET 值表，包括跑步、快走、游泳、骑行、跳绳、瑜伽、力量训练等常见运动。
- 添加运动记录时选择运动类型、输入时长，按 MET 自动估算消耗热量。
- 支持自定义运动：输入运动名称、时长和消耗热量。
- 今日运动列表展示已记录运动。
- 支持删除单条运动记录。
- 根据当天热量缺口推荐运动类型和时长。

### 体重 Tab

- 支持记录今日体重。
- 使用 Chart.js 展示最近 30 天体重趋势。
- 展示 BMI 和健康区间判断。
- 展示 BMR、TDEE 和减脂热量目标。
- 支持设置个人信息：性别、年龄、身高、当前体重、目标体重、活动水平。

### 饮水 Tab

- 提供快捷饮水记录按钮：+250ml、+350ml、+500ml。
- 展示今日饮水进度。
- 展示今日饮水记录列表。
- 支持撤销或删除饮水记录。

### 历史 Tab

- 支持选择日期查看历史数据。
- 展示指定日期的饮食、运动、饮水和体重记录。
- 支持通过日历或列表查看坚持情况。

### 关于弹窗

- 展示项目说明。
- 展示版本号。
- 说明 Mifflin-St Jeor 公式。
- 展示健康免责声明。

## 后端 API 设计

### 认证

- `POST /api/auth/register`：用户注册。
- `POST /api/auth/login`：用户登录。
- `POST /api/auth/logout`：退出登录。
- `GET /api/auth/me`：获取当前登录用户。

### 个人信息

- `GET /api/profile`：获取个人资料。
- `PUT /api/profile`：更新个人资料。

### 体重

- `GET /api/weight?days=30`：获取最近 N 天体重记录。
- `POST /api/weight`：添加体重记录。
- `DELETE /api/weight/:id`：删除体重记录。

### 饮食

- `GET /api/food/today`：获取今日饮食记录。
- `POST /api/food`：添加饮食记录。
- `DELETE /api/food/:id`：删除饮食记录。

### 自定义食物

- `GET /api/custom-foods`：获取用户自定义食物。
- `POST /api/custom-foods`：添加自定义食物。
- `DELETE /api/custom-foods/:id`：删除自定义食物。

### 运动

- `GET /api/exercise/today`：获取今日运动记录。
- `POST /api/exercise`：添加运动记录。
- `DELETE /api/exercise/:id`：删除运动记录。

### 饮水

- `GET /api/water/today`：获取今日饮水记录。
- `POST /api/water`：添加饮水记录。
- `DELETE /api/water/:id`：删除饮水记录。

### 历史

- `GET /api/history?date=YYYY-MM-DD`：获取指定日期的饮食、运动、饮水和体重记录。

## 数据表设计

- `users`：用户表，包含用户名、密码哈希和创建时间。
- `profiles`：个人资料表，包含性别、年龄、身高、体重、目标体重和活动水平。
- `weight_records`：体重记录表。
- `food_records`：饮食记录表。
- `custom_foods`：用户自定义食物表。
- `exercise_records`：运动记录表。
- `water_records`：饮水记录表。

## 计算公式

### BMR

Mifflin-St Jeor 公式：

```text
男性 BMR = 10 × 体重(kg) + 6.25 × 身高(cm) - 5 × 年龄 + 5
女性 BMR = 10 × 体重(kg) + 6.25 × 身高(cm) - 5 × 年龄 - 161
```

### TDEE

```text
TDEE = BMR × 活动系数
```

活动系数：

- 久坐：1.2
- 轻度活动：1.375
- 中度活动：1.55
- 高度活动：1.725

### 减脂目标热量

```text
减脂目标热量 = TDEE - 500kcal
```

### 食物热量

```text
总热量 = 每 100g 热量 × 食用量(g) / 100
```

### 运动消耗

```text
消耗热量 = MET × 体重(kg) × 时长(h)
```

### 减脂周期估算

```text
1kg 脂肪约等于 7700kcal
预计天数 = 目标减重热量缺口 / 每日热量缺口
```

## 关键约束

- 必须保持单页应用结构。
- 所有数据操作通过 fetch API 与后端通信。
- 不使用整页刷新。
- 后端所有业务接口必须校验 Session。
- 所有用户数据必须按 `user_id` 隔离。
- 后端返回统一 JSON 格式。
- 前端错误提示应展示后端返回的中文 `message`。
- 项目应可直接本地运行，不依赖额外服务。

