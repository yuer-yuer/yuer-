# MediaPlayer 项目 — C++11 新特性与设计模式面试指南

> 📅 整理日期：2026-06-04
> 🎯 适用场景：秋招面试、技术问答

---

## 一、C++11 新特性在项目中的应用

### 1. `nullptr` — 类型安全的空指针

**作用：** 替代 `NULL`/`0`，避免整数和指针的隐式转换歧义

**项目应用：**
```cpp
// ckernel.cpp — 初始化和清理
m_tcpClient = nullptr;  // 初始化
delete m_tcpClient;
m_tcpClient = nullptr;  // 置空防止野指针

// videocoverwidget.cpp — 空值检查
if (m_pMovie != nullptr) {
    m_pMovie->stop();
}
```

**面试回答模板：**
> "我项目中用 `nullptr` 替代了 `NULL`，因为 `NULL` 本质上是整数 0，在函数重载时可能产生歧义。比如 `void foo(int)` 和 `void foo(char*)`，传 `NULL` 会调用前者，而 `nullptr` 只能匹配指针类型，更安全。"

---

### 2. `auto` — 类型推导

**作用：** 编译器自动推导类型，简化复杂类型声明

**项目应用：**
```cpp
// TCPKernel.cpp — 迭代器推导
for (auto ite = m_mapFileIDToFileInfo.begin(); 
     ite != m_mapFileIDToFileInfo.end(); ++ite) {
    // 处理文件信息
}

// clogic.cpp — map 查找
auto ite = m_pKernel->m_mapFileIDToFileInfo.find(rq->m_nFileId);
```

**面试回答模板：**
> "项目中主要在遍历 `map` 容器时使用 `auto`，因为迭代器类型 `std::map<int, FileInfo*>::iterator` 很长，用 `auto` 让代码更简洁，也减少了手写类型出错的可能。"

---

### 3. Lambda 表达式 — 匿名函数

**作用：** 简化回调逻辑，避免为小功能单独写函数

**项目应用：**
```cpp
// playerdialog.cpp — RTMP 播放回调
connect(m_onlineDia, &OnlineDialog::SIG_playRtmp,
        this, [this](QString rtmp){
    m_player->stop(true);
    m_player->setFileName(rtmp);
    ui->lb_videoName->setText(rtmp);
    slot_PlayerStateChanged(PlayerState::Playing);
});

// uploaddialog.cpp — 录制完成回调
connect(m_uploadDlg, &RecoderDialog::SIG_recordFinished,
        this, [this](QString filePath) {
    // 处理录制完成逻辑
});
```

**面试回答模板：**
> "项目中 Lambda 主要用在 Qt 的 `connect` 里做信号槽回调。比如点击在线视频封面时，需要停止当前播放、设置新文件名、更新 UI 状态，这些逻辑如果写成单独的槽函数会很分散，用 Lambda 可以把相关逻辑集中在一起，可读性更好。捕获列表用 `[this]` 是为了访问成员变量。"

---

### 4. `override` — 显式重写标记

**作用：** 编译期检查虚函数签名是否正确，防止"以为重写了实际没重写"的错误

**项目应用：**
```cpp
// videocoverwidget.h
class VideoCoverWidget : public QWidget {
protected:
    void enterEvent(QEvent *event) override;   // 鼠标进入
    void leaveEvent(QEvent *event) override;   // 鼠标离开
    void paintEvent(QPaintEvent *event) override; // 绘制
};

// myopenglwidget.h
class MyOpenGLWidget : public QOpenGLWidget {
protected:
    void initializeGL() override;
    void paintGL() override;
    void resizeGL(int w, int h) override;
};
```

**面试回答模板：**
> "我在重写 Qt 虚函数时都加了 `override` 关键字。比如 `enterEvent`、`paintEvent` 这些，如果函数签名写错了（比如参数类型不对），没有 `override` 编译器不会报错，只是变成了一个新函数，运行时不会被调用。加了 `override` 后编译器会强制检查，签名不匹配就报错，避免了隐蔽的 bug。"

---

### 5. 范围 for 循环

**作用：** 简化容器遍历，语法更清晰

**项目应用：**
```cpp
// 遍历在线视频列表
for (auto &info : m_mapVideoIDToFileInfo) {
    // 直接使用 info
}
```

---

### 6. 列表初始化

**作用：** 统一的初始化语法，支持聚合类型

**项目应用：**
```cpp
// 结构体初始化
FileInfo info{0, "", 0, nullptr};
```

---

## 二、智能指针 — 项目改进建议

### 为什么项目目前用裸指针？

> 这是一个学习项目，初期为了理解内存管理原理用了裸指针。但实际开发中应该用智能指针，这也是我后续优化的方向。

### `unique_ptr` 适用场景 — 独占所有权

| 场景 | 说明 |
|------|------|
| `CKernel` 持有 `TcpClientMediator` | 只有 CKernel 使用，生命周期完全跟随 |
| 服务端 `FileInfo` 在 map 中 | map 是唯一拥有者，析构时自动释放 |
| `PlayerDialog` 持有 `VideoPlayer` | 播放器组件独占，界面销毁时一起释放 |

```cpp
// 改进示例：CKernel 持有网络中介
class CKernel {
    std::unique_ptr<TcpClientMediator> m_tcpClient;
    
    CKernel() : m_tcpClient(std::make_unique<TcpClientMediator>()) {
        // 不需要手动 delete
    }
};
```

### `shared_ptr` 适用场景 — 共享所有权

| 场景 | 说明 |
|------|------|
| `VideoState` 跨线程共享 | 主线程、视频线程、音频回调都需要访问 |
| `FileInfo` 被 map 和 UI 控件同时持有 | 数据库记录 + 界面展示双引用 |

```cpp
// 改进示例：VideoState 多线程共享
std::shared_ptr<VideoState> m_videoState = std::make_shared<VideoState>();

// 主线程、视频线程、音频回调都可以安全持有
void video_thread(std::shared_ptr<VideoState> vs) { /* ... */ }
void audio_callback(void* userdata) {
    auto vs = static_cast<std::shared_ptr<VideoState>*>(userdata);
}
```

**面试回答模板：**
> "我的项目目前用的是裸指针，但我清楚什么时候该用智能指针。`unique_ptr` 适合独占场景，比如 CKernel 持有 TcpClientMediator，只有 CKernel 用它；`shared_ptr` 适合共享场景，比如 VideoState 被主线程、视频线程、音频回调三个地方访问，需要引用计数来管理生命周期。用智能指针最大的好处是 RAII，即使发生异常也能自动释放内存，避免内存泄漏。"

---

## 三、设计模式在项目中的应用

### 1. 单例模式（Singleton）

**作用：** 保证一个类只有一个实例，并提供全局访问点

**项目应用：**
```cpp
// CKernel — 客户端核心
class CKernel {
public:
    static CKernel* GetInstance() {
        static CKernel instance;  // C++11 线程安全的局部静态变量
        return &instance;
    }
private:
    CKernel();   // 私有构造函数
    ~CKernel();
    CKernel(const CKernel&) = delete;            // 禁止拷贝
    CKernel& operator=(const CKernel&) = delete; // 禁止赋值
};

// TCPKernel — 服务端核心
class TcpKernel {
public:
    static TcpKernel* GetInstance();
};
```

**解决的问题：** 客户端只需一个网络核心，服务端只需一个内核实例管理所有连接

**面试回答模板：**
> "项目中 CKernel 和 TcpKernel 都用了单例模式。用的是 Meyers' Singleton，利用 C++11 局部静态变量的线程安全特性，第一次调用 `GetInstance()` 时才创建实例。构造函数私有化 + 删除拷贝构造，防止外部创建多个实例。"

---

### 2. 中介者模式（Mediator）

**作用：** 用一个中介对象封装一系列对象交互，使对象之间不需要显式引用

**项目应用：**
```cpp
// 抽象中介者接口
class INetMediator : public QObject {
public:
    virtual bool OpenNet(const char* ip, unsigned short port) = 0;
    virtual void CloseNet() = 0;
    virtual bool SendData(unsigned int ip, char* buf, int len) = 0;
    virtual void DealData(unsigned int ip, char* buf, int len) = 0;
    INet *m_pNet;
};

// 抽象网络接口
class INet {
public:
    virtual bool InitNet(const char* ip, unsigned short port) = 0;
    virtual void UnInitNet() = 0;
    virtual bool SendData(unsigned int ip, char* buf, int len) = 0;
    virtual void RecvData() = 0;
    INetMediator *m_pMediator;
};
```

**解决的问题：** CKernel 不直接依赖具体的 TCP/UDP 实现，通过 Mediator 抽象层解耦

```
CKernel ←→ INetMediator ←→ INet(TcpClient/UdpClient)
```

**面试回答模板：**
> "网络模块用了中介者模式。CKernel 不直接操作 socket，而是通过 INetMediator 接口。这样如果要从 TCP 切换到 UDP，只需要换一个 Mediator 实现，CKernel 代码完全不用改。核心思想是把'多对多'的依赖变成'一对多'。"

---

### 3. 策略模式（Strategy）

**作用：** 定义一系列算法，把它们封装起来，使它们可以互相替换

**项目应用：**
```cpp
// 服务端协议分发表
#define NetPackMap(a) TcpKernel::GetInstance()->m_NetPackMap[a - _DEF_PACK_BASE]

// 注册协议处理函数
void CLogic::setNetPackMap() {
    NetPackMap(_DEF_PACK_REGISTER_RQ) = &CLogic::RegisterRq;
    NetPackMap(_DEF_PACK_LOGIN_RQ)    = &CLogic::LoginRq;
    NetPackMap(_DEF_PACK_UPLOAD_RQ)   = &CLogic::UploadRq;
    NetPackMap(_DEF_PACK_DOWNLOAD_RQ) = &CLogic::DownloadRq;
    // ...
}

// 收到数据包时分发
PFUN pfun = m_NetPackMap[type - _DEF_PACK_BASE];
if (pfun) {
    (this->*pfun)(ip, buf, len);
}
```

**解决的问题：** 用函数指针数组替代巨大的 `switch-case`，新增协议只需注册一行

**面试回答模板：**
> "服务端用了策略模式。维护一个函数指针数组 `m_NetPackMap`，每个协议类型对应一个处理函数。收到数据包后通过类型号直接查表调用，时间复杂度 O(1)。比 switch-case 好在：新增协议只需注册，不用改分发逻辑，符合开闭原则。"

---

### 4. 观察者模式（Observer）

**作用：** 定义对象间一对多的依赖关系，当一个对象状态改变时，所有依赖者都会收到通知

**项目应用：**
```cpp
// Qt 信号槽机制就是观察者模式的实现
signals:
    void SIG_playRtmp(QString rtmp);  // 信号：播放 RTMP
    void SIG_recordFinished(QString filePath);  // 信号：录制完成

slots:
    void slot_PlayerStateChanged(PlayerState state);  // 槽：状态改变

// 连接信号槽
connect(m_onlineDia, &OnlineDialog::SIG_playRtmp,
        this, &PlayerDialog::onPlayRtmp);
```

**解决的问题：** UI 控件之间通过信号槽通信，不需要直接引用对方

**面试回答模板：**
> "项目中大量使用 Qt 的信号槽机制，本质就是观察者模式。比如在线视频列表点击后要通知播放器，但 OnlineDialog 不需要知道 PlayerDialog 的存在，只需要发信号，由框架负责分发。这实现了完全的解耦。"

---

### 5. 生产者-消费者模式（Producer-Consumer）

**作用：** 解决生产速度和消费速度不匹配的问题

**项目应用：**
```cpp
// 线程池 + 环形缓冲区任务队列
class ThreadPool {
    TaskQueue* m_taskQueue;      // 任务队列（环形缓冲区）
    pthread_mutex_t m_lock;      // 互斥锁
    pthread_cond_t m_notEmpty;   // 非空条件变量
    pthread_cond_t m_notFull;    // 非满条件变量
};

// 生产者：主线程添加任务
void ThreadPool::addTask(Task* task) {
    pthread_mutex_lock(&m_lock);
    while (m_taskQueue->isFull()) {
        pthread_cond_wait(&m_notFull, &m_lock);  // 队列满则等待
    }
    m_taskQueue->push(task);
    pthread_cond_signal(&m_notEmpty);  // 通知消费者
    pthread_mutex_unlock(&m_lock);
}

// 消费者：工作线程取任务
Task* ThreadPool::getTask() {
    pthread_mutex_lock(&m_lock);
    while (m_taskQueue->isEmpty()) {
        pthread_cond_wait(&m_notEmpty, &m_lock);  // 队列空则等待
    }
    Task* task = m_taskQueue->pop();
    pthread_cond_signal(&m_notFull);  // 通知生产者
    pthread_mutex_unlock(&m_lock);
    return task;
}
```

**解决的问题：** 主线程读取网络数据（生产）速度快，解码/处理（消费）速度慢，用队列缓冲

**面试回答模板：**
> "项目中线程池用了生产者消费者模式。主线程收到网络数据后作为生产者把任务放进环形缓冲区，工作线程作为消费者取任务处理。用互斥锁保护共享队列，条件变量实现同步——队列满时生产者阻塞，队列空时消费者阻塞。这样既避免了忙等待，又保证了线程安全。"

---

### 6. 责任链模式（Chain of Responsibility）

**作用：** 将请求沿着处理链传递，每个处理者决定处理或转发

**项目应用：**
```cpp
// PlayerDialog — 事件过滤器处理滑块点击
bool PlayerDialog::eventFilter(QObject *obj, QEvent *event) {
    if (obj == ui->slider_progress) {
        if (event->type() == QEvent::MouseButtonPress) {
            QMouseEvent *mouseEvent = static_cast<QMouseEvent*>(event);
            // 计算点击位置对应的进度
            double pos = mouseEvent->pos().x() / (double)ui->slider_progress->width();
            m_player->seek(pos);
            return true;  // 事件已处理，不再传递
        }
    }
    return QWidget::eventFilter(obj, event);  // 继续传递
}

// 安装事件过滤器
ui->slider_progress->installEventFilter(this);
```

**解决的问题：** 在不修改 Qt 控件源码的情况下，拦截并处理特定事件

**面试回答模板：**
> "项目中用了 Qt 的事件过滤器，本质是责任链模式。比如我想让进度条支持点击跳转，但 QSlider 默认不支持。通过 `installEventFilter` 把 PlayerDialog 安装为事件过滤器，鼠标点击事件会先经过 `eventFilter`，我在里面处理点击位置计算并调用 seek，返回 true 表示事件已消费。这样不需要继承 QSlider 重写，更灵活。"

---

## 四、设计模式对比总结

| 设计模式 | 核心思想 | 项目应用场景 | 解决的问题 |
|---------|---------|-------------|-----------|
| 单例 | 全局唯一实例 | CKernel, TcpKernel | 全局核心对象管理 |
| 中介者 | 中间人解耦 | INetMediator ↔ INet | 网络层与业务层解耦 |
| 策略 | 算法封装替换 | m_NetPackMap[] | 协议分发，避免 switch |
| 观察者 | 发布-订阅 | Qt 信号槽 | UI 组件间通信解耦 |
| 生产者-消费者 | 缓冲区同步 | 线程池任务队列 | 读写速度不匹配 |
| 责任链 | 请求链式传递 | eventFilter | 事件拦截处理 |

---

## 五、面试高频追问

### Q1: 为什么用函数指针数组而不是 switch-case？
> 函数指针数组符合开闭原则。新增协议类型只需注册一行代码，而 switch-case 需要修改分发函数。而且数组查找是 O(1)，switch 编译后也是跳转表，性能差不多，但维护性更好。

### Q2: 单例模式的线程安全问题？
> C++11 标准规定局部静态变量的初始化是线程安全的（§6.7），所以 Meyers' Singleton 在 C++11 下天然线程安全，不需要加锁。

### Q3: Qt 信号槽和传统观察者模式的区别？
> Qt 信号槽有类型安全检查（编译期）、支持跨线程通信（自动排队到目标线程的事件循环）、支持 Lambda 回调。传统观察者模式通常用虚函数回调，没有这些特性。

### Q4: 生产者消费者模式中为什么用 while 而不是 if 判断条件？
> 因为可能存在虚假唤醒（spurious wakeup），操作系统可能在条件未满足时唤醒线程。用 while 可以确保唤醒后再次检查条件，避免误操作。

---

*文档结束 — 祝面试顺利！*
