# 📋 今日计划清单 实施提示词

> 替换概览Tab的"任务排序"为"今日计划清单"，支持添加/删除/完成/超时/进度/AI小瘦生成计划

---

## 一、整体替换

将概览Tab中"任务排序"按钮及面板，替换为"今日计划清单"。

- 原按钮：`任务排序` → 拖拽排序面板（删掉）
- 新按钮：`今日计划清单` → 计划清单面板
- 按钮样式保持新粗野主义不变，文字替换

---

## 二、面板结构

```
┌─────────────────────────────────────┐
│  ───────（拖动条）                    │
│  TODAY'S PLAN                [×]   │
│  今日计划清单                        │
│  ═════════════════════════════════  │
│                                     │
│  ┌─────────────────────────────┐   │
│  │ 🤖 让小瘦制定计划            │   │  ← AI生成按钮
│  └─────────────────────────────┘   │
│                                     │
│  ☐ 记录早餐          截止 09:00    │  ← 待完成
│  ☑ 喝水500ml         截止 10:00  ✓ │  ← 已完成
│  ☐ 运动30分钟        截止 18:00    │  ← 待完成
│  ☒ 晚间称重          已过期 22:00  │  ← 超时
│                                     │
│  💡 常用（点击添加）：               │  ← 空清单时显示
│  [记录三餐] [喝水] [运动] [称重]    │
│                                     │
│  ┌──────────────────┬──────────┐   │
│  │ 添加任务...       │ 时间(选) │   │  ← 输入行
│  └──────────────────┴──────────┘   │
│                                     │
│  ████████░░░░  完成 2/4 (50%)      │  ← 进度条
└─────────────────────────────────────┘
```

---

## 三、任务项三种状态

| 状态 | 视觉 | 交互 |
|------|------|------|
| **待完成** | 黑色粗边框，白背景，☐空方框 | 点击☐ → 完成 |
| **已完成** | 绿色粗边框+浅绿背景，☑打勾 | 点击☑ → 取消完成 |
| **已过期** | 灰色粗边框+浅灰背景，删除线文字，截止时间标红"已过期" | 不可点击，只能删除 |

### 任务项布局（从左到右）

```
[☐/☑]  任务名称          截止 HH:MM    [×]
```

- 勾选框：粗边框方框
- 任务名：粗体
- 截止时间：小字灰色；过期时红色+"已过期"
- 删除：×号，hover显示

---

## 四、添加任务

- 输入框 + 可选截止时间 + 添加按钮
- 截止时间点击弹出简单时间选择（小时:分钟），可选
- 回车键也可直接添加
- 添加后清空输入框，新任务出现在列表

### 空清单预设建议

清单为空时显示常用提示（不自动添加，点击才添加）：

```
💡 常用计划（点击添加）：
[记录三餐] [喝水2000ml] [运动30分钟] [称重记录]
```

---

## 五、删除任务

- 每个任务右侧 × 删除按钮（hover显示）
- 点击直接删除 + toast "已删除" + 撤销按钮（3秒内可撤销）

---

## 六、进度显示

```
████████░░░░  完成 3/5 (60%)
```

- 粗边框长条，绿色填充(#6bcb77)，灰色底色
- 右侧：完成数/总数 + 百分比
- 100%时满格 + "🎉 全部完成！"
- 已过期项计入分母（视为未完成）

---

## 七、AI小瘦制定计划（核心联动）

### 按钮

- 位置：清单列表上方第一个元素
- 样式：粗边框大按钮，浅蓝背景，🤖 + "让小瘦制定计划"
- 点击：关闭清单面板 → 打开小瘦对话面板 → 自动触发生成

### 生成流程

1. 用户点击"让小瘦制定计划"
2. 关闭清单面板，打开小瘦对话面板
3. 小瘦自动显示："好的！让我根据你的数据来制定今日计划~ 🤔"
4. 小瘦读取用户今日饮食/运动/体重数据
5. 小瘦生成编号1-5的计划清单：

```
📋 今日减脂计划：
1. ☐ 早餐记录（9:00前）
2. ☐ 喝水1000ml（12:00前）
3. ☐ 午餐记录（13:00前）
4. ☐ 运动30分钟（18:00前）
5. ☐ 晚间称重（22:00前）

检查一下，没问题的话点下面添加 👇
```

6. 显示"✅ 添加到今日计划"按钮
7. 用户点击 → 5条任务自动添加到今日计划清单
8. toast提示"已添加5项计划！"

### 计划生成规则

| 数据来源 | 影响 |
|----------|------|
| 当前时间 | 早上→全天计划；下午→下午+晚间；晚上→晚间 |
| 今日饮食记录 | 缺哪餐安排记录哪餐 |
| 今日运动记录 | 未运动则安排运动 |
| 目标进度 | 接近目标时多鼓励性任务 |
| 用户偏好 | 常做运动/常吃食物相关任务 |

### 生成后交互

- 用户可和小瘦对话修改（"把运动改成游泳"/"去掉称重"）
- 小瘦修改后重新展示完整计划
- 确认后点"添加到今日计划"一键添加
- 已有相同名称的任务不重复添加

---

## 八、超时检测

- 打开面板时检查一次
- 面板打开期间每60秒检查一次
- 当前时间超过截止时间 → "待完成"变为"已过期"
- 已完成的不变
- 面板关闭时不检查

---

## 九、数据存储

### localStorage方案

- 功能简单，每日清零，无需数据库
- key: `daily_plan_2026-05-19`（日期拼接）
- value: 任务数组 `[{id, text, deadline, done, expired}]`
- 每次打开面板时检查日期，非今天则清旧数据

---

## 十、功能边界

- 不做拖拽排序
- 不做分类标签
- 不做子任务
- 不做重复/周期任务
- 不做提醒推送
- 每天计划独立，不跨天
