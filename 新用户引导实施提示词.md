# 🎯 新用户引导（Onboarding）实施提示词

> 新用户注册后首次进入时，弹出分步引导面板，收集基本信息，完成后不再弹出

---

## 一、触发机制

### 判断条件

- profiles 表新增 `onboarded` 字段（INTEGER DEFAULT 0）
- 用户登录/注册成功后，前端请求 `/api/profile`
- 如果 `onboarded === 0`，自动弹出引导面板
- 引导完成时，前端调用 `PUT /api/profile` 更新所有信息 + `onboarded=1`
- 已引导用户（`onboarded === 1`）直接进首页，永不弹出

### 不可跳过

- 引导面板**无关闭按钮、无跳过按钮**
- 必须完成所有步骤才能进入App
- 点击遮罩层不关闭面板

---

## 二、引导步骤（3步，合并同类项）

7个字段合并成3步，每步信息量适中，不枯燥：

### 第1步：基本信息

```
┌─────────────────────────────────────┐
│  👋 欢迎来到慢慢瘦！                │
│  让我们了解你，才能更好地帮你         │
│                                     │
│  你的性别                           │
│  ┌──────┐  ┌──────┐                │
│  │  🙋‍♂️  │  │  🙋‍♀️  │                │
│  │  男生  │  │  女生  │               │
│  └──────┘  └──────┘                │
│                                     │
│  你的年龄                           │
│  ┌─────────────────────┐            │
│  │  22                 │            │
│  └─────────────────────┘            │
│                                     │
│  你的身高（cm）                      │
│  ┌─────────────────────┐            │
│  │  175                │            │
│  └─────────────────────┘            │
│                                     │
│         [ 下一步 → ]                │
└─────────────────────────────────────┘
```

- 性别：两个粗边框方形按钮，选中态黄底黑字
- 年龄：数字输入框，范围 12-100
- 身高：数字输入框，范围 100-250，带单位cm

### 第2步：体重目标

```
┌─────────────────────────────────────┐
│  🎯 设定你的目标                     │
│                                     │
│  当前体重（kg）                      │
│  ┌─────────────────────┐            │
│  │  75                 │            │
│  └─────────────────────┘            │
│                                     │
│  目标体重（kg）                      │
│  ┌─────────────────────┐            │
│  │  65                 │            │
│  └─────────────────────┘            │
│                                     │
│  目标日期                           │
│  ┌─────────────────────┐            │
│  │  2026-08-22         │            │
│  └─────────────────────┘            │
│  💡 健康减脂建议每周0.5-1kg          │
│                                     │
│    [ ← 上一步 ]  [ 下一步 → ]       │
└─────────────────────────────────────┘
```

- 当前体重：数字输入框，范围 30-300，带单位kg
  - 提交时同时写入 `weight`、`initial_weight` 两个字段
  - 同时插入一条 `weight_records` 记录（作为起始体重）
- 目标体重：数字输入框，必须小于当前体重
- 目标日期：日期选择器，最小值为明天
- 底部提示：根据当前体重和目标体重差值，动态显示建议时长
  - 差值 ≤ 5kg → "建议4-6周"
  - 差值 5-10kg → "建议6-12周"
  - 差值 10-20kg → "建议12-24周"
  - 差值 > 20kg → "建议24周以上，稳扎稳打"

### 第3步：活动等级

```
┌─────────────────────────────────────┐
│  🏃 你平时的活动量                   │
│                                     │
│  ┌─────────────────────────────┐    │
│  │ 🪑 久坐不动                  │    │
│  │ 办公室工作，几乎不运动         │    │
│  └─────────────────────────────┘    │
│  ┌─────────────────────────────┐    │
│  │ 🚶 轻度活动                  │    │
│  │ 每周1-3天有运动              │    │
│  └─────────────────────────────┘    │
│  ┌─────────────────────────────┐    │
│  │ 🏋️ 中度活动                  │    │
│  │ 每周3-5天有运动              │    │
│  └─────────────────────────────┘    │
│  ┌─────────────────────────────┐    │
│  │ 🔥 高强度运动                 │    │
│  │ 每周6-7天有运动              │    │
│  └─────────────────────────────┘    │
│  ┌─────────────────────────────┐    │
│  │ 💪 极高强度                  │    │
│  │ 体力劳动或每天高强度训练       │    │
│  └─────────────────────────────┘    │
│                                     │
│    [ ← 上一步 ]  [ 开始使用 ✨ ]    │
└─────────────────────────────────────┘
```

- 5个选项，纵向排列，每个是粗边框卡片
- 选中态：黄底黑字 + ✓
- 对应值：sedentary / light / moderate / active / very_active
- 点击"开始使用"后提交所有数据

---

## 三、提交逻辑

点击"开始使用"时，一次性提交全部数据：

**调用 `PUT /api/profile`**，body 包含：
```json
{
  "gender": "male",
  "age": 22,
  "height": 175,
  "weight": 75,
  "initial_weight": 75,
  "target_weight": 65,
  "target_date": "2026-08-22",
  "activity_level": "moderate",
  "onboarded": 1
}
```

**额外操作（后端处理）：**
- 同时插入一条 `weight_records` 记录，record_date 为今天
- 根据 profile 数据计算 BMR/TDEE/每日目标，写入每日目标缓存

**提交成功后：**
- 面板滑出消失
- 首页正常加载，所有数据已就绪
- 页面顶部出现一条短暂 toast："🎉 设置完成，开始你的减脂之旅！"

---

## 四、步骤导航

- 顶部进度条：3段，当前步骤高亮为黄色，已完成为黑色，未到为浅灰
- "上一步"按钮：回到前一步，保留已填数据
- "下一步"按钮：当前步必填项校验通过后可点击
- 每步切换有左右滑动动画

---

## 五、校验规则

| 字段 | 规则 | 错误提示 |
|------|------|---------|
| 性别 | 必选 | "请选择性别" |
| 年龄 | 12-100 的整数 | "请输入有效年龄（12-100）" |
| 身高 | 100-250 的数字 | "请输入有效身高（100-250cm）" |
| 当前体重 | 30-300 的数字 | "请输入有效体重（30-300kg）" |
| 目标体重 | 必须小于当前体重 | "目标体重应小于当前体重" |
| 目标日期 | 必须是明天及之后 | "目标日期至少是明天" |
| 活动等级 | 必选 | "请选择活动等级" |

校验失败时，输入框变红边框 + 下方显示错误文字，不弹 alert。

---

## 六、UI 风格

遵循新粗野主义移动端 App 风格：
- 面板全屏覆盖，白色/浅黄底，3px 黑边框
- 所有输入框：3px 黑边框、2px 圆角、硬阴影
- 按钮：黄底黑字、3px 黑边框、hover 时硬阴影偏移 2px
- 选中态：黄底 + ✓ 标记
- 进度条：粗线条（4px），黄黑配色
- 标题字体加粗放大，步骤之间左右滑动切换

---

## 七、后端改动

### profiles 表新增字段

在 `profileColumns` 数组中新增：
```javascript
{ name: 'onboarded', type: 'INTEGER', defaultValue: '0' }
```

### PUT /api/profile 扩展

- 接收 `onboarded` 字段并写入数据库
- 当 `onboarded` 从 0 变为 1 时，同时执行：
  1. 插入 `weight_records`（当前体重，日期为今天）
  2. 计算并返回 BMR/TDEE/每日目标值

### GET /api/profile 响应

- 返回的 profile 对象中包含 `onboarded` 字段
- 前端据此判断是否弹出引导面板
