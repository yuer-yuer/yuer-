# 🏠 用户模块架构重组实施提示词

> 删除体重/历史Tab，整合到"我的"Tab，概览Tab功能增强，新粗野主义风格

---

## 一、整体架构调整

### Tab栏调整（5个Tab）

```
修改前：概览 | 饮食 | 运动 | 体重 | 饮水 | 历史
修改后：概览 | 饮食 | 运动 | 饮水 | 我的
```

### "我的"Tab入口

- **底部Tab栏**：新增"我的"Tab，点击进入用户模块主页
- **右上角头像**：App顶部导航栏右侧显示用户头像，点击同样进入"我的"Tab
- 两个入口功能完全一致

---

## 二、概览Tab增强

### 新增：今日体重卡片（可点击）

**位置**：在现有四个核心卡片（运动/饮食/体重/饮水）下方

**卡片内容**：
- 左上角显示"今日体重"标签
- 大号数字显示当前体重（取最新记录）
- 右侧小字显示"目标体重"和剩余天数进度
- 底部进度条显示减重进度百分比
- 点击可记录今日体重或设置目标

**交互**：
- 点击卡片 → 弹出记录今日体重面板（底部滑出）
- 面板内有"设置目标"入口（复用现有弹窗）
- 输入体重、日期，确认后更新卡片数据

### 新增：体重提醒设置卡片

**位置**：今日体重卡片下方

**卡片内容**：
- 左侧闹钟图标 + "每日称重提醒"
- 右侧开关 + 提醒时间显示（如"08:00"）
- 下方小字提示"提醒有助养成好习惯"

**交互**：
- 开关控制浏览器通知权限
- 点击时间 → 弹出时间选择器（底部滑出，滑动选择小时和分钟）
- 设置后显示"下次提醒：明天 08:00"

---

## 三、"我的"Tab主页设计

"我的"Tab是一个完整的页面，不是底部滑出面板。

### 页面结构（新粗野主义风格）

```
┌─────────────────────────────────────┐
│  [← 返回]        我的        [头像] │  ← 顶部导航（可选）
├─────────────────────────────────────┤
│                                     │
│  ┌─────────────────────────────────┐│
│  │                                 ││
│  │    [大头像]                     ││
│  │                                 ││
│  │      用户名                     ││
│  │   累计记录X天 · 加入XX天         ││
│  │                                 ││
│  └─────────────────────────────────┘│
│         ↑ 粗野主义大卡片             │
│                                     │
│  ┌────────┬────────┬────────┐      │
│  │  私教  │  计划  │  档案  │      │
│  │  🤖   │  🎯   │  📊   │      │
│  └────────┴────────┴────────┘      │
│         ↑ 粗边框按钮组               │
│                                     │
│  ┌─────────────────────────────────┐│
│  │     📅 历史记录          →      ││
│  └─────────────────────────────────┘│
│  ┌─────────────────────────────────┐│
│  │     📈 打卡统计          →      ││
│  └─────────────────────────────────┘│
│         ↑ 粗边框大按钮               │
│                                     │
│  ═════════════════════════════════  │
│                                     │
│  设置                               │
│  ─────────────────────────────────  │
│  关于我们                           │
│  退出登录                           │
│                                     │
└─────────────────────────────────────┘
```

### 新粗野主义UI特征

- **粗边框**：所有卡片、按钮使用3-4px粗边框（黑色或深色）
- **硬阴影**：卡片和按钮使用硬边阴影（box-shadow无blur，xy偏移明显）
- **高对比**：黑白对比为主，点缀色用于强调（红/黄/蓝）
- **大字体**：关键数字用大号粗体字
- **无圆角或极小圆角**：保持硬朗感
- **不对称布局**：可适当打破对称

---

## 四、"我的"Tab功能项

### 1. 我的私教（AI陪练）

**当前阶段**：仅UI占位，后续接入AI对话功能

**入口样式**：
- 粗边框方形按钮
- 内部显示机器人/AI相关emoji
- 标签文字"AI私教"

**点击后**：弹窗提示"AI教练功能即将上线，敬请期待！"（底部滑出提示或中间弹窗）

### 2. 我的计划

**入口样式**：
- 粗边框方形按钮
- 内部显示目标/靶心emoji
- 标签文字"我的计划"

**点击后**：底部滑出设置面板

**面板内容**：
- 标题"设置减脂目标"
- 当前体重（只读，取最新记录）
- 目标体重输入框（kg）
- 目标日期选择（日期选择器）
- AI建议计划预览（每日摄入目标、每日运动目标、周期天数）
- 粗边框保存按钮

### 3. 身体档案

**入口样式**：
- 粗边框方形按钮
- 内部显示图表/数据emoji
- 标签文字"身体档案"

**点击后**：底部滑出身体档案面板

**面板内容**（新粗野主义风格卡片）：

| 卡片 | 内容 | 可编辑 |
|------|------|--------|
| 基础信息 | 昵称、性别、年龄、身高 | 是 |
| 身体数据 | 当前体重、BMI、BMR、目标体重 | 部分 |
| 减脂进度 | 起始体重、已减重量、剩余目标、完成百分比 | 否（自动计算）|
| 体重趋势 | 近7天/30天体重变化折线缩略图 | 否 |

**BMI显示规则**：
- 根据BMI值显示状态标签（偏瘦/正常/偏胖/肥胖）
- 用颜色区分（绿/黄/橙/红），高对比粗体字

**BMR显示规则**：
- 根据性别、年龄、身高、体重自动计算
- 显示基础代谢率数值 + "千卡/天"

### 4. 历史记录

**入口样式**：
- 粗边框长方形大按钮
- 左侧日历emoji，右侧文字"历史记录"+箭头
- 占据整行宽度

**点击后**：底部滑出日历面板

**日历面板内容**：
- 月份标题+左右切换箭头
- 日历网格（7列），日期数字粗体
- 有记录的日期用高对比色块标记
- 点击日期后，日历下方显示该日期的四信息卡片

**四信息卡片内容**（新粗野主义风格）：

```
┌──────────────────────────────────┐
│ 体重                              │ ← 粗体标签
│ ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━ │ ← 粗分割线
│ 78kg                              │ ← 大号粗体数字
└──────────────────────────────────┘
┌──────────────────────────────────┐
│ 饮食                              │
│ ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━ │
│ 午 白米饭(100g/116kcal)          │
└──────────────────────────────────┘
┌──────────────────────────────────┐
│ 运动                              │
│ ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━ │
│ 游泳(自由泳) 1分钟 -3kcal        │
│ 力量训练(轻) 30分钟 -140kcal     │
└──────────────────────────────────┘
┌──────────────────────────────────┐
│ 饮水                              │
│ ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━ │
│ 💧 总计 2300ml (6次)             │
└──────────────────────────────────┘
```

**交互细节**：
- 默认显示当天数据
- 点击日期后该日期高亮边框
- 无数据的日期显示"暂无记录"
- 卡片支持点击展开查看详情列表

### 5. 打卡统计

**入口样式**：
- 粗边框长方形大按钮
- 左侧图表emoji，右侧文字"打卡统计"+箭头
- 占据整行宽度

**点击后**：底部滑出统计报告面板

**统计报告面板内容**：

**Tab切换**：本周 | 本月 | 全部（粗边框按钮组）

**指标摘要卡片**（横向排列3个粗边框卡片）：
- 累计打卡天数 / 总记录天数
- 平均每日热量摄入 / 目标完成率
- 累计运动消耗 / 累计减重

**体重趋势图**：
- 折线图，显示所选时间段内的体重变化
- X轴：日期，Y轴：体重(kg)
- 目标体重线用虚线标注
- 粗线条，高对比配色

**每日热量趋势图**：
- 柱状图或折线图
- 显示每日摄入热量 vs 目标热量
- 超标日期用橙色粗边框标注

**饮食类型分布**：
- 饼图或环形图
- 早餐/午餐/晚餐/加餐 各餐次热量占比
- 粗边框分隔

**运动类型分布**：
- 横向条形图
- 按运动类型统计累计时长/消耗
- 粗边框条形

**连续打卡日历**（小尺寸）：
- 迷你日历样式
- 有记录的日期用高对比色块标记

---

## 五、交互设计要点

### 弹窗层级

```
App主界面
  └── "我的"Tab主页（完整页面）
       ├── 身体档案（底部滑出）
       ├── 历史记录-日历（底部滑出）
       ├── 打卡统计（底部滑出）
       └── 我的计划-设置（底部滑出）
```

### 关闭逻辑

- 点击外部蒙层或拖动条向下拖动 → 关闭当前底部面板
- 底部面板关闭后回到"我的"Tab主页

### 数据同步

- "我的"Tab中的数据与现有数据库表同步
- profiles表、weight_records表、food_records表、exercise_records表、water_records表
- 修改个人信息后即时同步到全局状态

### 空状态处理

- 历史记录：无记录时显示粗边框提示卡片"开始记录你的第一天吧！"+粗边框引导按钮
- 体重趋势：数据不足时显示"记录更多数据后生成趋势图"
- 打卡统计：无数据时显示"坚持记录，数据会在这里呈现"

---

## 六、新粗野主义UI风格规范

### 核心特征

- **边框**：3-4px solid black（或深灰#1a1a1a）
- **阴影**：硬阴影 `box-shadow: 4px 4px 0 #000`（无blur）
- **圆角**：极小（2px）或零圆角
- **背景**：纯白或米白，卡片用纯色块（黄/粉/蓝/绿）
- **字体**：粗体为主，关键数字用大号（text-3xl以上）
- **间距**：紧凑但不拥挤，留白用硬分割线代替

### 配色方案

| 用途 | 颜色 | 说明 |
|------|------|------|
| 边框/文字 | #1a1a1a 或纯黑 | 高对比 |
| 背景 | #ffffff 或 #fef9e7 | 米白纸张感 |
| 强调色1 | #ff6b6b | 红，用于警告/重要 |
| 强调色2 | #ffd93d | 黄，用于高亮 |
| 强调色3 | #6bcb77 | 绿，用于成功/达标 |
| 强调色4 | #4d96ff | 蓝，用于信息/链接 |

### 组件样式

**粗边框卡片**：
```
border: 3px solid #1a1a1a;
box-shadow: 4px 4px 0 #1a1a1a;
border-radius: 2px;
background: #ffffff;
```

**粗边框按钮**：
```
border: 3px solid #1a1a1a;
box-shadow: 3px 3px 0 #1a1a1a;
border-radius: 2px;
background: #ffd93d; /* 或白/粉/蓝 */
font-weight: bold;
```

**硬分割线**：
```
border-top: 2px solid #1a1a1a;
margin: 1rem 0;
```

---

## 七、移动端适配

- 底部滑出面板占屏幕高度85%-90%
- 日历组件支持左右滑动切换月份
- 图表在移动端使用响应式尺寸
- 按钮触摸区域不小于48px
- 粗边框按钮在移动端更醒目

---

## 八、功能边界说明

- **我的私教**：本期只做UI占位，AI功能后续迭代
- **体重提醒**：复用现有浏览器Notification机制
- **数据统计**：使用Chart.js CDN渲染图表（粗线条、高对比配色）
- **数据导出**：本期不做，后续考虑
